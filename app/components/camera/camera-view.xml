<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="onNavigatingTo">
  <ActionBar title="Escanear Nota Fiscal" />
  <GridLayout rows="*, auto">
    <Camera id="camera" class="camera" saveToGallery="false" />
    <Button text="Capturar" tap="{{ onCapture }}" class="btn btn-primary" row="1" />
  </GridLayout>
</Page>